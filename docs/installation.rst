============
Installation
============

At the command line::

    $ easy_install django-publica-admin

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-publica-admin
    $ pip install django-publica-admin
